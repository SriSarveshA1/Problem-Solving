# Write your MySQL query statement below
select q.query_name , round(avg(q.rating/q.position),2) as quality,coalesce(round(sum(case when rating <3 then 1 end)/count(*) * 100,2),0)  as poor_query_percentage  from queries q group by q.query_name;